# Core dependencies (required)
requests>=2.31.0
pandas>=2.0.0
python-dotenv>=1.0.0

# Excel export support (optional but recommended)
openpyxl>=3.1.0

# Progress bars (optional)
tqdm>=4.66.0

# Scheduler support (optional)
schedule>=1.2.0

# Google Sheets integration (required for daily delivery)
gspread>=5.12.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# Date/time utilities
python-dateutil>=2.8.2
pytz>=2023.3

# Database support (optional - only if using database)
# sqlalchemy>=2.0.0

# Note: Selenium, BeautifulSoup, and lxml are NOT needed
# The scraper uses the Publix API directly, no HTML parsing required
